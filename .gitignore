node_modules/
dist/
.env
*.log
.DS_Store

# Internal specs, workflow diagrams and runbook — kept local, not on GitHub.
# Patterns, not a file list, so a newly added flow or branch spec is ignored
# by default rather than only after someone remembers to add it here.
/*.svg
UC*-MOCK-APIS.md
PROJECT-BRIEFING.md
OPEN-ITEMS-REGISTER.md
DEPLOYMENT.md
